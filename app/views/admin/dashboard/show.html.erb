<div class="mx-auto container-md text-center">
  <h1 class="mb-5 mt-5">Admin Dashboard</h1>
  <div class="card w-25 text-center mb-4 mx-auto">
    <p class="card-title">Current Product Count:</p>
    <p class="card-text"><%= @products_count %></p>
  </div>
  <div class="card w-25 text-center mx-auto">
    <pclass="card-title">Current Category Count:</p>
    <p class="card-text"><%= @categories_count %></p>
  </div>
</div>